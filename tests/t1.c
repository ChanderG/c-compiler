//Hi, simple test file.
void main(){
  int a,b;
  char c,d;
  int e,f;
  double g,h;
  x = 2;
  //y = 5;

  a = x*y;
  //a = x / y;
  //a = x + y;
  //a = x - y;
  ++a;

  //a = +x;
  a = -x;

  //a < x;
  //a > x;
  //a >= x;
  //a <= x;
  //a == x;
  //a != x;

  //(a<=x) && (a>=x) ;
  a < x && a > x;
  a < x || a > x;

}
